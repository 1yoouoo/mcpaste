# Copy to .env.local, then source/export it before running locally.
# Never place paste content or real credentials in this file.

MCPASTE_ENV=development
MCPASTE_HTTP_ADDR=:8080
MCPASTE_LOG_LEVEL=info
