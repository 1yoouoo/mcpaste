# Copy to /etc/mcpaste/postgres.env on the host, set root:root ownership, and chmod 0600.
# Keep this file limited to PostgreSQL bootstrap variables; do not add application keys.
POSTGRES_DB=mcpaste
POSTGRES_USER=mcpaste
POSTGRES_PASSWORD=REPLACE_WITH_RANDOM_PASSWORD
